
<body>
    <div class="layout-wrapper layout-content-navbar">
        <div class="layout-container">
            <%- include('../../../../../partials/sideBar') %>
            <div class="layout-page">
                <%- include('../../../../../partials/header') %>
                <div class="content-wrapper">
                    <div class="admin-container">
                        <div class="admin-header">
                          <h2>Vendor Discount Code List</h2>
                            <% if(adminPerms.includes('vendor-discount-code-create')) { %>
                                <a href="/vendor/discount-code/add" class="btn btn-gradient">Add New</a>
                            <% } %>
                        </div>

                        <div class="row g-4 mb-4">
                            <div class="col-md-12 col-lg-12">
                                <div class="stats-card">
                                    <div class="card-body">
                                        <div class="">
                                            <table class="table table-striped table-hover" id="discountCodeTable">
                                                <thead>
                                                    <tr>
                                                        <th>#</th>
                                                        <th>Code</th>
                                                        <th>Discount Value</th>
                                                        <th>Status</th>
                                                        <th>Created</th>
                                                        <th>Actions</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <% if(discountCodes.length > 0){ %>
                                                        <% discountCodes.forEach((type, index) => { %>
                                                            <tr>
                                                                <td><%= index + 1 %></td>
                                                                <td><%= type.codeName %></td>
                                                                <td><%= type.codeValue %>%</td>
                                                                <td>
                                                                    <% if(adminPerms.includes('vendor-discount-code-status')) { %>
                                                                        <div class="form-check form-switch">
                                                                            <input class="form-check-input status-toggle" 
                                                                                type="checkbox" 
                                                                                data-id="<%= type._id %>"
                                                                                <%= type.status ? 'checked' : '' %>>
                                                                        </div>
                                                                    <% } %>
                                                                </td>
                                                                <td> 
                                                                    <%= formatDate(type.createdAt) %>
                                                                </td>
                                                                <td>
                                                                    <% if(adminPerms.includes('vendor-discount-code-edit')) { %>
                                                                        <a href="/vendor/discount-code/<%= type._id %>/edit" class="btn btn-sm btn-icon btn-outline-primary ms-1"><i class="bx bx-edit"></i> </a>
                                                                    <% } %>
                                                                </td>
                                                            </tr>
                                                        <% }); %>
                                                    <% }; %>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                  <%- include('../../../../../partials/footer') %>
                  <div class="content-backdrop fade"></div>
                </div>
            </div>
        </div>
        <div class="layout-overlay layout-menu-toggle"></div>
    </div> 

    <script>
        $(document).ready(function () {
            $('#discountCodeTable').DataTable({
                responsive: true
            });

             $('.status-toggle').change(function() {
                const discountCodeId = $(this).data('id');
                console.log(discountCodeId);
                const isChecked = $(this).is(':checked');
                
                $.ajax({
                    url: `/vendor/discount-code/${discountCodeId}/status`,
                    type: 'PUT',
                    data: { status: isChecked },
                    success: function(response) {
                        if (response.success) {
                            Swal.fire({
                                icon: 'success',
                                title: 'Success!',
                                text: response.message,
                                timer: 1500,
                                showConfirmButton: false
                            });
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'Error!',
                                text: response.message
                            });
                            // Revert the toggle if failed
                            $(this).prop('checked', !isChecked);
                        }
                    },
                    error: function(xhr) {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error!',
                            text: xhr.responseJSON?.message || 'Failed to update status'
                        });
                        // Revert the toggle on error
                        $(this).prop('checked', !isChecked);
                    }
                });
            });
        });
  </script>

</body>

