
<body>
    <div class="layout-wrapper layout-content-navbar">
        <div class="layout-container">
            <%- include('../../../../partials/sideBar') %>
            <div class="layout-page">
                <%- include('../../../../partials/header') %>
                <div class="content-wrapper">
                    <div class="admin-container">
                        <div class="admin-header">
                          <h2>Property Type List</h2>
                            <% if(adminPerms.includes('property-type-create')) { %>
                                <a href="/property-type/add" class="btn btn-gradient">Add New</a>
                            <% } %>
                        </div>

                        <div class="row g-4 mb-4">
                            <div class="col-md-12 col-lg-12">
                                <div class="stats-card">
                                    <div class="card-body">
                                        <div class="">
                                            <table class="table table-striped table-hover" id="propertyTypeTable">
                                                <thead>
                                                    <tr>
                                                        <th>#</th>
                                                        <th>Name</th>
                                                        <th>Short Stay Fee</th>
                                                        <th>Long Stay Fee</th>
                                                        <th>Propertys</th>
                                                        <th>Image</th>
                                                        <th>Status</th>
                                                        <th>Created</th>
                                                        <th>Actions</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <% if(propertyTypes.length > 0){ %>
                                                        <% propertyTypes.forEach((type, index) => { %>
                                                            <tr>
                                                                <td><%= index + 1 %></td>
                                                                <td><%= type.name %></td>
                                                                <td><%= type.cleaningFees?.shortStay || 0 %>%</td>
                                                                <td><%= type.cleaningFees?.longStay || 0 %>%</td>
                                                                <td>
                                                                    <span class="badge bg-primary"><%= type.properties ? type.properties.length : 0 %></span>
                                                                    <!-- <% if (type.properties.length > 0) { %>
                                                                        <div class="mt-1">
                                                                        <% type.properties.slice(0, 3).forEach(property => { %>
                                                                            <span class="badge bg-light text-dark me-1">
                                                                            <%= property.name %>
                                                                            </span>
                                                                        <% }); %>
                                                                        <% if (type.properties.length > 3) { %>
                                                                            <span class="badge bg-secondary">+<%= type.properties.length - 3 %> more</span>
                                                                        <% } %>
                                                                        </div>
                                                                    <% } %> -->
                                                                </td>
                                                                <td>
                                                                    <% if (type.image) { %>
                                                                        <a href="<%= type.image %>" target="_blank">
                                                                            <img src="<%= type.image %>" alt="<%= type.name %>" style="max-height: 50px;">
                                                                        </a>
                                                                    <% } else { %>
                                                                        No Image
                                                                    <% } %>
                                                                </td>
                                                                <td>
                                                                    <% if(adminPerms.includes('property-type-status')) { %>
                                                                        <div class="form-check form-switch">
                                                                            <input class="form-check-input status-toggle" 
                                                                                type="checkbox" 
                                                                                data-id="<%= type._id %>"
                                                                                <%= type.status ? 'checked' : '' %>>
                                                                        </div>
                                                                    <% } %>
                                                                </td>
                                                                <td><%= formatDate(type.createdAt) %></td>
                                                                <td>
                                                                    <% if(adminPerms.includes('property-type-show')) { %>
                                                                        <a href="/property-type/<%= type._id %>/show" class="btn btn-sm btn-icon btn-outline-primary"><i class="bx bx-show"></i> </a>
                                                                    <% } %>
                                                                    <% if(adminPerms.includes('property-type-edit')) { %>
                                                                        <a href="/property-type/<%= type._id %>/edit" class="btn btn-sm btn-icon btn-outline-primary ms-1"><i class="bx bx-edit"></i> </a>
                                                                    <% } %>
                                                                </td>
                                                            </tr>
                                                        <% }); %>
                                                    <% }; %>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                  <%- include('../../../../partials/footer') %>
                  <div class="content-backdrop fade"></div>
                </div>
            </div>
        </div>
        <div class="layout-overlay layout-menu-toggle"></div>
    </div> 

    <script>
        $(document).ready(function () {
            $('#propertyTypeTable').DataTable({
                responsive: true
            });

             $('.status-toggle').change(function() {
                const propertyTypeId = $(this).data('id');
                console.log(propertyTypeId);
                const isChecked = $(this).is(':checked');
                
                $.ajax({
                    url: `/property-type/${propertyTypeId}/status`,
                    type: 'PUT',
                    data: { status: isChecked },
                    success: function(response) {
                        if (response.success) {
                            Swal.fire({
                                icon: 'success',
                                title: 'Success!',
                                text: response.message,
                                timer: 1500,
                                showConfirmButton: false
                            });
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'Error!',
                                text: response.message
                            });
                            // Revert the toggle if failed
                            $(this).prop('checked', !isChecked);
                        }
                    },
                    error: function(xhr) {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error!',
                            text: xhr.responseJSON?.message || 'Failed to update status'
                        });
                        // Revert the toggle on error
                        $(this).prop('checked', !isChecked);
                    }
                });
            });
        });
  </script>

</body>

