
<body>
    <div class="layout-wrapper layout-content-navbar">
        <div class="layout-container">
            <%- include('../../../partials/sideBar') %>
            <div class="layout-page">
                <%- include('../../../partials/header') %>
                <div class="content-wrapper">
                    <div class="admin-container">
                        <div class="admin-header">
                             <h2>Concierge Services</h2>
                             <% if(adminPerms.includes('concierge-service-create')) { %>
                                <a href="/concierge-service/add" class="btn btn-gradient">Add New</a>
                             <% } %>
                        </div>

                        <div class="row g-4 mb-4">
                            <div class="col-md-12 col-lg-12">
                                <div class="stats-card">
                                    <div class="card-body">
                                        <div class="">
                                            <table class="table table-striped table-hover" id="servicesTable">
                                                <thead>
                                                    <tr>
                                                        <th>#</th>
                                                        <th>Name</th>
                                                        <th>Image</th>
                                                        <th>Status</th>
                                                        <th>Created</th>
                                                        <th>Actions</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    <% if(services.length > 0){ %>
                                                          <% services.forEach((service, index) => { %>
                                                            <tr>
                                                                <td><%= index + 1 %></td>
                                                                <td><%= service.name %></td>
                                                                <td>
                                                                    <% if (service.image) { %>
                                                                        <a href="<%= service.image %>" target="_blank">
                                                                            <img src="<%= service.image %>" alt="<%= service.name %>" style="max-height: 50px;">
                                                                        </a>
                                                                    <% } else { %>
                                                                        <span class="text-muted">No Image</span>
                                                                    <% } %>
                                                                </td>
                                                                <td>
                                                                    <% if(adminPerms.includes('concierge-service-status')) { %>
                                                                        <div class="form-check form-switch">
                                                                            <input class="form-check-input status-toggle" 
                                                                                type="checkbox" 
                                                                                data-id="<%= service._id %>"
                                                                                <%= service.status ? 'checked' : '' %>>
                                                                        </div>
                                                                    <% } %>
                                                                </td>
                                                                <td> 
                                                                    <%= formatDate(service.createdAt) %>
                                                                </td>
                                                                <td>
                                                                    <% if(adminPerms.includes('concierge-service-view')) { %>
                                                                        <a href="/concierge-service/<%= service._id %>/show" class="btn btn-sm btn-icon btn-outline-primary"><i class="bx bx-show"></i> </a>
                                                                    <% } %>
                                                                    <% if(adminPerms.includes('concierge-service-edit')) { %>
                                                                        <a href="/concierge-service/<%= service._id %>/edit" class="btn btn-sm btn-icon btn-outline-primary ms-1"><i class="bx bx-edit"></i> </a>
                                                                    <% } %>
                                                                </td>
                                                            </tr>
                                                        <% }); %>
                                                    <% }; %>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                  <%- include('../../../partials/footer') %>
                  <div class="content-backdrop fade"></div>
                </div>
            </div>
        </div>
        <div class="layout-overlay layout-menu-toggle"></div>
    </div> 

<script>
    $(document).ready(function() {
        $('#servicesTable').DataTable();
        
        $('.status-toggle').change(function() {
            const serviceId = $(this).data('id');
            const isChecked = $(this).is(':checked');
            
            $.ajax({
                url: `/concierge-service/${serviceId}/status`,
                type: 'PUT',
                data: { status: isChecked },
                success: function(response) {
                    if (response.success) {
                        Swal.fire({
                            icon: 'success',
                            title: 'Success!',
                            text: response.message,
                            timer: 1500,
                            showConfirmButton: false
                        });
                    } else {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error!',
                            text: response.message
                        });
                        // Revert the toggle if failed
                        $(this).prop('checked', !isChecked);
                    }
                },
                error: function(xhr) {
                    Swal.fire({
                        icon: 'error',
                        title: 'Error!',
                        text: xhr.responseJSON?.message || 'Failed to update status'
                    });
                    // Revert the toggle on error
                    $(this).prop('checked', !isChecked);
                }
            });
        });
    });
</script>

</body>

